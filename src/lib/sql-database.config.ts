// Database configuration for SQL Database

const dbConfig = {
    host: 'localhost',
    port: 5432,
    user: 'username',
    password: 'password',
    database: 'mydatabase'
};

export default dbConfig;